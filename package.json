{
  "name": "@web3hub/ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "rm -rf .next out && next build && next export",
    "deploy": "cd out && surge && cd .."
  },
  "dependencies": {
    "next": "^10.0.5",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "theme-ui": "^0.9.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.8",
    "typescript": "^4.2.4"
  }
}
